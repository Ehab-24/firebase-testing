
import 'package:flutter/material.dart';

class Shadows{

  static const List<BoxShadow> appBar = [
      BoxShadow(
        blurRadius: 5,
        offset: Offset(0, 3),
        color: Colors.white70,
      ),
      BoxShadow(
        blurRadius: 10,
        color: Colors.black,
      )
    ];
}